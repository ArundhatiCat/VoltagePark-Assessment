sudo docker build -f Dockerfile.gpu -t fastapi-video:gpu .


sudo docker run --rm -it --gpus all --ipc=host --shm-size=16g \
  -p 8000:8000 \
  -v "$PWD/models-cache:/app/.cache/huggingface" \
  fastapi-video:gpu


